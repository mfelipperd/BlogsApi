/* const { categorie } = require('../models');

const newCategorie = async (name) => {
    const categorieCreated = await categorie.create(name);
    console.log(name);
    // const newCategorieName = await categorie.findOne({ where: { name } });
    return categorieCreated;
};

module.exports = {
newCategorie,
};
*/